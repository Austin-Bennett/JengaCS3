package physics;

import game.GameObject;

public class PhysicsObject extends GameObject {

    /*
    * Create methods/fields for:
    * position, velocity, acceleration, and mass
    * methods for applying a force
    * methods for manipulating velocity and acceleration (addVelocity/addAccleration) etc
    * */


    @Override
    public void update(double deltaTime) {
        //update all physics in here (add velocity to position, acceleration to velocity, reset velocity)
    }
}
